DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/medical_app
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRE_MINUTES=480
UPLOAD_DIR=uploads
ACCESS_GRANT_DAYS=30
# Optional: enables Claude-powered chat intake (/intake/message).
# Without it the endpoint still works using the built-in rule parser.
ANTHROPIC_API_KEY=
