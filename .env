# FastAPI backend (My-Health-Compass/backend). Start it with:
#   cd backend && python -m uvicorn app.main:app --reload --port 8000
VITE_API_URL="http://localhost:8000"

# Optional: AI features (Ingestion Hub, ToxCheck, nutrition, in-app search)
# use the Lovable AI gateway. Set this to enable them; unset just disables AI.
# LOVABLE_API_KEY=""
